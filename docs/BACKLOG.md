# Backlog

Open work with no single line to mark. `rg -n 'BUG:|TODO:'` lists the rest.

Immediate order after the completed list/provenance work:

1. Distinguish a definitive tier-2 absence from a transient probe failure, then prevent the latter from silently falling through to synthesis; the remaining product choice is an interactive confirmation versus uniform refusal with explicit `--generate` as the override.
2. Make every page in an upstream release bundle uninstall as one managed unit, including checksum protection and restoration of each displaced vendor page.
3. Admit bounded documentation roots inside monorepos, using OpenCode's versioned English docs as the first real fixture.

## Recovered from an unrecorded session (2026-09-09)

Four things were settled or raised in conversation on 2026-09-09 and never written down; they were reconstructed from that session's transcript on 2026-09-10 and are recorded here so they stop depending on anyone's memory. Where a decision was provisional, that is said rather than smoothed over.

- **Done (`677442c`); kept for the naming reasoning.** The `Source` column's `install-root` value did not explain itself, and is now `vendor` -- the word `installer.py` already used for a non-MANIAC page ("A foreign or vendor manpage already exists at ..."), so the vocabulary is consistent across the codebase. `upstream` stays reserved for the tier-2 case, a page from the project's own repository, and was deliberately not introduced yet. The path here is worth knowing: `shipped` was agreed first and provisionally -- "shipped for now, but i dont love it" -- and deliberately not executed on those grounds; `vendor` was chosen later from a comparison of rendered tables. Only the display enum moved (`cli/listing.py`, `PageSource.VENDOR`); `Tier.INSTALL_ROOT` in `manifest.py` is a different concept and is **persisted in users' manifests on disk**, so renaming it would orphan every existing entry -- it was confirmed distinct and left alone.
- Remove `--generate` from `install`, or justify it. Asked directly -- "why do we need `maniac install --generate` and not `maniac install`?" -- and the conclusion reached was that there is no good reason, but nothing was proposed or changed. The flag restricts selection to tier 3 (`orchestration/install.py:62,71,98`), which ADR-0016's authoritative-first tier order makes a strictly worse outcome for an LLM call that was not needed: tiers 1 and 2 record a version too, so forcing synthesis can only buy a poorer page. That argues for deletion rather than a default flip. What it would cost is the ability to regenerate a page deliberately when a shipped one exists but is bad -- which is the "judge whether an existing manpage is worth replacing" milestone below, so the two should be decided together rather than separately.
- **Done, and it earned its keep: tier 2 is proven and a masking bug was found.** Both shipped pages were deleted (`~/.local/share/mise/installs/fzf/0.74.3/fzf.1`, `~/.local/share/mise/installs/tmux/3.7b/tmux.1`; restore with `mise install fzf --force` / `mise install tmux --force`), removing tier 1 so tier 2 had to prove itself.
  **`fzf` passed.** `maniac install fzf` printed `upstream manpage from repository (0.74.3)`, cloned `junegunn/fzf` at the matching tag, and installed the genuine upstream page -- 86KB at `~/.local/share/man/man1/fzf.1`, carrying Junegunn Choi's MIT header -- with the manifest recording `tier: repository`, `source: junegunn/fzf`, `version: 0.74.3`. **No LLM call was made**; tier 3 was never reached. This is the first end-to-end confirmation that clone-and-glob tier 2 works against a real repository, and it corroborates the `extra_assets` answer key, which names `man/man1/fzf.1` as the right page.
  **`tmux` failed as predicted at the time**, confirming that the installation's `tmux/tmux-builds` provenance was not the documentation-canonical repository. This was resolved by `726338b`, `4c3dc14`, and `86cc6d2`: the exact packaged-default mapping preserves the build repository as installation provenance and sends every documentation consumer to `tmux/tmux`, without a suffix heuristic or Mise `extra_assets`.
- **A failed tier 2 silently proceeds to tier-3 synthesis, which will mask exactly the bug above.** Found while running the experiment, and it sharpens the "quiet degradation" entry above from a reporting nicety into a correctness problem. When tier 2 found nothing in `tmux-builds`, MANIAC did not stop -- it fell through to tier 3 and made a real, billed LLM call against `gemini/gemini-3.7-flash`. That call happened to fail with a Gemini 503, so the visible error was a compound of two unrelated causes and the tier-2 bug stayed legible by luck. **On a day the API is healthy, tier 3 would have succeeded and `maniac install tmux` would have reported success while installing a synthesized page instead of the upstream one that exists at `tmux/tmux`.** Nobody would have looked again. So the tmux mis-resolution is not self-reporting, and neither is any other tier-2 failure: synthesis is a plausible-looking cover for all of them. This argues that falling through from a *failed* tier 2 is different from falling through from an *absent* tier 2 -- the first means something MANIAC believed was reachable was not, and ADR-0016's authoritative-first ordering does not by itself say the fallback should be silent. Decide whether a tier-2 failure should refuse rather than synthesize, or at minimum name the degradation in the outcome line, before trusting any `install` result on a tool whose upstream resolution is unverified.
- **Done (`3db9008`, `a504e42`, `1948c2a`, `0dfb834`).** `get_help` used to accept empty stdout, discard stderr, and ignore a failed `--help`; tmux exposed the defect by printing a real usage block to stderr and exiting 1. Help crawling now handles the streams separately on success, validates combined output from a failed invocation, accepts it only when it contains an anchored `usage:` line, and rejects empty or option-error-only output. Separating the streams initially regressed successful `--version` output written to stderr; `a504e42` restored the previous combined-stream behavior for that path.
- **Done, with the original refusal narrowed (`3db9008`).** Tier 3 now refuses only when neither usable help nor repository documentation exists. Root help alone remains useful when no manpage exists, so it generates with a limited-source warning rather than failing; repository documentation alone can also generate when the help crawl fails. Every synthesis reports the command and subcommand counts, repository identity, documentation-file count, and version-match result before calling the LLM. The stronger idea of judging whether arbitrary context is sufficiently relevant was not adopted: repository identity is a separate provenance problem, and a minimum-size rule would reject small but authoritative inputs.
- **Telling "wrong repository" from "repository legitimately ships no manpage".** These are different failures needing different responses -- the first is a resolution bug, the second is normal and should proceed to synthesis -- and MANIAC currently cannot distinguish them, which is why the `tmux` failure looked like an ordinary miss. Reporting does not depend on solving this: naming the tier used and the repository consulted in the outcome line (`tmux -- synthesized, tmux/tmux-builds carried no page`) makes a human spot the bug immediately, and is the cheap fix worth doing first. For the distinction itself, the measurement is the flag-inventory overlap described above: near-zero overlap between the binary's advertised flags and anything in the cloned tree means the repository is not about this tool, while partial overlap with no roff file means the right repository genuinely ships no page. A weaker corroborating signal, useful but not sufficient alone: a packaging repository contains no implementation source at all -- `tmux-builds` holds only `Dockerfile`, `docker-compose.yml`, `scripts/`, `versions.env`, `LICENSE` and `README.md`.
- Decide whether `Config` may freeze the XDG directories at import time. Raised as "`discovery.py` duplicates XDG resolution, route it through `Config`" and investigated on 2026-09-10; **the investigation inverted the entry, so the original framing is wrong and is kept here only so nobody re-derives it.** The two resolutions are byte-identical in formula -- same env var, same precedence, same default, no `expanduser` on either side -- and differ only in *when* they run. `config.py:20-23` computes `_XDG_CONFIG`/`_XDG_CACHE`/`_XDG_DATA`/`_XDG_STATE` as module-level constants at first import and never revisits them; `discovery.py:208,287` reads `os.environ` live on every call. So routing discovery through `Config` would not remove a duplicate, it would silently make discovery stop honouring an env var set after `maniac.config` was imported -- and `cli/__init__.py` builds a module-level `default_cfg = Config()` at import, so that import always happens first.
  Nothing in `maniac/` mutates the XDG variables at runtime (`os.environ` is written in exactly one place, `config.py:18`, for an unrelated LiteLLM setting), so **no production behaviour is wrong today**: a normal CLI run has its environment fixed before the process starts and both readings agree.
  What is wrong is that `Config` cannot be steered by the environment once imported, which makes it untestable the ordinary way. `tests/conftest.py`'s autouse `_no_real_xdg_writes` exists precisely to work around this, monkeypatching the frozen module globals directly because "an ordinary env-var monkeypatch after import has no effect" -- and the comment records that the leak reappears at any entry point building a default `Config`. The result is a suite where `monkeypatch.setenv("XDG_CONFIG_HOME", ...)` steers `discovery` but not `Config`, which is a trap rather than a convenience.
  So the open decision is about `Config`, not about `discovery`: either `Config` stops freezing these at import (resolve per-instance in the `default_factory`, which touches every consumer and is the riskier change), or import-time freezing is declared intentional and `discovery`'s live read is brought into line deliberately rather than incidentally. Worth an ADR either way, because the choice is about how configuration is bound throughout, not about two lines. Note that whichever way it goes, `conftest`'s workaround is evidence for the first option: the suite already had to reach past the public interface to do a normal thing.

- **Done for claimed binaries (ADR-0019); the unclaimed half moved to ADR-0020.** Tier-3 synthesis used to record no version at all, so a MANIAC-generated page could never read `outdated`. `run_pipeline` now resolves the installed version, threads it into documentation extraction so the repository clone is tag-matched, and records the version only when that match succeeded. Verified live: regenerating `aichat` produced a `v0.30.0` clone and an entry recording `"version": "0.30.0"`.
  What remains is the case where *no provider claims the binary at all* — then there is no `Installation` and no version, so the page still reads `ok` permanently. ADR-0020 decides that one: record the binary's own `--version` output verbatim and treat any change as `outdated`. See `docs/STATE.md` for that work.
- Disambiguate `list` rows that collapse to the same visible label. `_grouped_for_display` (`maniac/cli/listing.py:343`) keys groups on `(provider, package, state, source, upstream)` but labels a solo group with its *tool* name and a multi-binary group with its *package* name, so one package split across two states can render two rows a reader cannot tell apart: observed live as `node | available` next to `node (2 binaries) | missing` (the package is `node`; the solo row is the `node` binary, the pair is `npm`/`npx`). Same shape for `python (9 binaries) | missing` sitting apart from `python3.14 | available`. The label carries no way to act on it either -- neither row names the binaries to pass to `install`. Piped output is unaffected (bare binary names, one per line), so this is display-only. Fix direction not settled: either label solo rows with the package name plus the binary (`node (node)`), or list binary names up to a width cap before falling back to the count.
- **Done (`ADR-0027`).** Source keywords are OSC-8 links to the exact reachable or shipped local manpage, or, for GitHub, to the version-pinned upstream file/release asset rather than MANIAC's cache. The Upstream column remains a repository link. Tier-2 discovery records the remote URI beside cached pages and installation persists it in the manifest. Unsupported terminals and unsupported Git hosts still show plain keywords; compressed pages may open as files rather than rendered manuals.
- Add exact-file Source-link adapters for non-GitHub repository hosts as real installations require them. Generic Git probing accepts arbitrary HTTPS clone URLs, but browser paths are host-specific (`/-/blob/` on GitLab, `/src/` on Bitbucket), so deriving one without identifying the host would create confident broken links. Keep the Source keyword plain until an adapter can be verified against that host.
- Recover documentation nested in monorepos without reopening every dependency tree. OpenCode v1.18.30 is the concrete fixture: its published site is `https://opencode.ai/docs`, and the matching repository tag contains the English source under `packages/web/src/content/docs`, but `extract_docs_from_dir` currently ignores `packages/` wholesale. Admit bounded, explicit documentation roots such as `packages/*/docs` and `packages/*/src/content/docs`, prefer the default language, and keep `node_modules`, build products, translations, and unrelated workspace packages excluded.
- Treat a multi-page upstream release as one uninstallable installation. Tier 2 currently records each companion page under its own manual name (for example `eza`, `eza_colors`, and `eza_colors-explanation`), so checksum protection and vendor-backup restoration work for every page, but `maniac uninstall eza` removes only the primary entry; companions require separate uninstall commands. Persist a bundle owner or installation id and remove/restore the whole recorded bundle when its primary tool is uninstalled.
- Add a dense-table row guide if it improves real terminal output. Rich supports `show_lines=True`, `leading=1`, alternating `row_styles`, and selected `end_section` separators. Lines or leading space double an already tall table, so the plausible default is a theme-safe alternating row style; test it in light and dark terminals before choosing one.
- Add external-page freshness adapters for Arch, RPM-family systems, and Homebrew only when each can be exercised against a real installation. ADR-0026 intentionally ships Debian's cached `dpkg-query` ownership/version proof first; unsupported systems report externally resolved pages as `unverified` rather than guessing from paths or package names.
- Split the `system` Source label into package-owned and other external pages, or rename it. `system` currently means only "outside the active installation root": Debian owns the mismatched `tldr` page, but `yadm` resolves `~/.local/lib/yadm/yadm.1`, a user-owned page whose 3.5.0 header currently matches the Mise yadm 3.5.0 binary. The latter is correctly not called stale without package proof, but `system` misstates its location. A conservative roff-header verifier could turn an exact title/package and version match into `ok` and a mismatch into `outdated`; it must reject ambiguous headers rather than scan arbitrary version-looking text.
- Act on the `outdated` state: regenerate a MANIAC-managed page whose recorded version no longer matches the installed binary. ADR-0018 makes `outdated` visible in `list` but decides nothing about the upgrade path -- whether `install` overwrites in place, whether a vendor backup is re-taken, and whether a tier-3 regeneration is worth its LLM call when the binary moved only a patch version. Depends on the manifest repair entry below, since nothing can be shown outdated until entries carry a real version.
  A version mismatch is the cheap signal, not the only one: recording generation inputs would also let a page be judged stale because its documentation context drifted, catching a rewritten `--help` under an unchanged version number.
- Repair the manifest entries that predate its version field by re-deriving the pages MANIAC owns. ADR-0018 makes `outdated` a State requiring positive evidence -- a recorded page version differing from the installed binary's -- and chose repair over carrying an `unknown` value through the interface, so every entry needs a real recorded version rather than a stamped guess. Two entries exist today (`~/.local/state/maniac/installed.json`: `aichat` and `ty`, both `tier: synthesis`), so the repair costs two LLM calls; a machine with many managed pages pays proportionally, and the pass should be resumable rather than all-or-nothing. Stamping existing entries with the current binary version was explicitly rejected: it asserts a freshness nobody checked. Note the permanent case this does not fix and is not meant to -- `local_lib` never reports a version, so a page for a raw checkout can never be shown outdated and reads `ok` by design.
  Necessary but not sufficient on its own: both entries are `tier: synthesis`, and the tier-3 path still records no version for *new* installs either (see the synthesis-version entry above), so repairing these two without that fix buys one `outdated`-capable pair of rows and no more.
- Extend installed package-metadata fallback beyond npm. Legacy Mise npm installs now inspect an unambiguous `node_modules/<package>/package.json` or `lib/node_modules/<package>/package.json` before registry inference; this resolves `bash-language-server` to `bash-lsp/bash-language-server` without an install-ID prefix or Mise `extra_assets`. Equivalent installation-derived fallback remains open for Python, Cargo, Go, and Homebrew metadata. Registry APIs remain a fallback only after package identity is established, never from a bare executable name. Candidate discovery must stay stricter than this and keep requiring installation-derived evidence rather than name-only registry attribution -- the previous name-only approach produced real collisions (`fmt` -> `nushell/nufmt`, `od` -> `todotxt/todo.txt-cli`, GNU `envsubst` -> `a8m/envsubst`).
- Add package-provenance discovery for system candidates. On Debian-family systems, resolve the exact manpage path with `dpkg-query -S -- PATH`, then inspect the owning package's source package, homepage/VCS fields, and copyright; a package's VCS field can name a downstream distribution repo rather than upstream, so the source model must keep that distinction rather than presenting it as upstream. Cache or batch the lookups -- a global manpath scan can hold hundreds of candidate pages. This also reaches commands that are package-owned but not on `$PATH`, such as `/usr/share/misc/config.guess`.
- Extract bounded documentation from an installed package root as well as a cloned repository. Read every eligible documentation file at the package root and under recognized documentation directories (e.g. `bash-language-server`'s package root already has an unread top-level `README.md`), excluding dependency/build/generated/test trees such as `node_modules`. Add bounded local sources for system-installed candidates too -- `/usr/share/doc/<package>/README*`/`NEWS`/examples, `/usr/share/info/<tool>.info*`, the package description as supplementary-only context, and the package-owned executable's `--help` via its absolute path -- keeping provenance (upstream repo vs. installed package vs. package manager vs. Info vs. local help) distinct in the generated context.
- Verify `maniac compare` (`evaluation.judge.compare_manpages`) against a live LLM and a real installed manpage. It is unit-tested with `run_llm_synthesis` mocked only; the `fzf` markdown/context pair already staged for an earlier aborted comparison attempt (see `docs/STATE.md`) is ready to use once the API is available.
- Do not use linuxcommandlibrary.com as a documentation source; researched and rejected. Its content is not scraped from distro packages but generated into `assets/commands/*.md` (9035 files) in `SimonSchubert/LinuxCommandLibrary` by a single author, with no `/about` page, no AI-disclosure either way, and `CONTRIBUTING.md` telling contributors to submit only command names because "we'll take care of creating the documentation". Only 1805 of 9035 entries carry a `# RESOURCES` block naming any upstream source. The corpus is a lossy paraphrase -- its `od` entry drops `--traditional` and two synopsis forms; its `bluetoothctl` entry omits `-s/--init-script` and `-z/--zsh-complete` and recommends the upstream-deprecated `hcitool`. The Apache-2.0 `LICENSE` also covers source code only, with the README leaving manpage copyright to "their respective authors", so there is no content licence -- though that matters only if generated pages are ever distributed, not for local use. The rejection rests on provenance quality, not licensing. The page for a tool like `bluetoothctl` is genuinely richer than the 2018 help2man stub installed here, which is exactly what makes it dangerous: the delta is unattributed and of unproven provenance.
- Add tldr-pages as an EXAMPLES-section source. It is the only surveyed online source that is both provenance-sound and absent from a stock Debian install: `CONTRIBUTING.md` carries an explicit anti-LLM policy ("Pull requests suspected of being made in whole or in part through generative AI ... will be closed"), contributors sign a CLA, and it is CC-BY 4.0, which for locally installed pages imposes nothing (obligations attach on distribution), and is the cheapest terms available should that ever change. Bulk zip at `github.com/tldr-pages/tldr/releases/latest/download/tldr.zip`, no scraping. It carries real-world invocation examples, which neither a manpage nor an info page provides. Note this machine already has `tealdeer` with 7444 cached pages, so a local client should be preferred over the network where present,; crediting it in MANIAC's provenance header is good practice rather than a requirement for local use.
- Consider the Arch Wiki for integration and configuration prose. Additive in a dimension nothing else covers -- its `Bluetooth` article is 7360 words where the installed `bluetoothctl.1` is a 2018 help2man stub -- and available as raw wikitext through the MediaWiki API. Its cost is structural, not legal: it has no per-command organisation (`bluetoothctl` itself is a 5-word redirect, the content lives inside task articles), so extracting per-command material is the hard part. GFDL 1.3 was previously recorded here as probably disqualifying; that was wrong. Copyright obligations attach on distribution, and a page generated for and installed on the user's own machine is private use. Licensing only becomes a constraint if generated pages are committed, packaged, or shipped as fixtures.
- Do not fetch man7.org, manpages.debian.org, manpages.ubuntu.com or the GNU online manuals; measured as redundant with local disk. `signal(7)` differs from man7.org only by man-pages version (6.7 local vs 6.18) and line wrapping; `od.1` from manpages.ubuntu.com is 0.957-identical to the installed page with the residual being site chrome; the gnu.org `od` node differs from the local info node by two sentences of 9.11 changelog. Fetching these buys version drift, not information. One real gap: man7.org is additive for sections 2 and 3 when `manpages-dev` is absent (as here -- `man 2 open` fails), but the cheaper fix is installing that package, not an HTTP fetch.
- Feed the installed manpage into generation as a documentation source. Even a page MANIAC judges worth replacing carries authoritative material that `--help` crawling and repository docs do not always yield: exact option spellings, the full flag inventory, default values, exit statuses and section structure. `gum`'s 986 flag entries are accurate data even though the page around them is a flag dump. The classification layer already reads and decompresses every page (`manpages.read_manpage_source`), so supplying it costs nothing extra once a tool is selected. Two constraints. Provenance must stay distinct in the assembled context, as the bounded-local-sources item above already requires, so the model can weigh an upstream-authored page differently from a generated one -- the classification facts already say which it is. And a poor page risks anchoring the model to the structure MANIAC is trying to improve on, so the prompt should present it as a reference for flags and defaults rather than as a model of how the page should read; this wants checking against `maniac compare` before it is trusted.
- Count flag entries marked with `.HP` rather than `.TP`, if a reliable rule can be found. `bat.1` marks each option with `.HP` followed by an `.IP` description and contains no `.TP` at all, so it reports `tp_count=0` and the candidate heuristic abstains on it; `fish-lsp.1` is the same shape. This is safe rather than wrong -- an unmeasurable page is never selected -- but a genuinely poor `.HP`-style page is equally invisible. Two measured dead ends, so a third attempt should not repeat them. Counting `.HP` unconditionally fails: of 3144 pages reporting no flag entries, 438 contain `.HP`, but nearly all are a single `.HP` used as a SYNOPSIS idiom (`apt`, `NetworkManager`, the `Xcup*(3)` family), so the count would be 1 and the words-per-flag ratio meaningless. Counting rendered options by regex over bold hyphen runs fails worse: it finds 5 options in `fzf.1`, which has 128, and none at all in `fd`, `yadm`, `zoxide` or `tmux`, because pages differ in whether they escape the hyphen and whether the bold macro is inline or on its own line. A rule would need to distinguish a per-option `.HP` from a synopsis `.HP` structurally, for instance by requiring several and by checking the following line is a description rather than a continuation.
- Verify the npm, pipx and Homebrew providers' `detect()` against a real installation. All three are written from documented layouts and have never been run against one, which [ADR-0015](adr/0015-installer-provider-model.md) names as the known weak point of the provider model. Cargo and go were verified live against a real `cargo install hexyl` and `go install goimports`; these three cannot be, because none of the three package managers works on the development system -- pipx and Homebrew are absent, and mise-managed npm is broken (`MODULE_NOT_FOUND` on `npm-cli.js`). Partial coverage exists and should not be mistaken for none: npm's and pipx's `resolve_source` parsing (npm `package.json` `repository`, pipx dist-info `METADATA`) is exercised for real through mise backend composition, which resolved `yaml-language-server` and `tlpui`. It is specifically the standalone `detect()` path -- `<prefix>/lib/node_modules/<pkg>/`, `$PIPX_HOME/venvs/<pkg>/`, and the Homebrew Cellar symlink shape -- that is assumed. Homebrew is wholly unverified, `resolve_source` included. The layouts are stable and documented, so the likely defect is a wrong path assembly or a missing edge case rather than a wrong design.

- Measure what refusing an unmatched upstream version actually costs. `install`'s tier 2 refuses outright when the installed version is unknown or no matching git tag exists (`docs/adr/0016-authoritative-manpages-first.md`), falling through to synthesis rather than installing a page from the default branch under a false version claim. That refusal is consistent with never guessing, but the retired roadmap flagged its coverage cost as unmeasured and asked for the measurement before the choice was settled -- the choice was made provisionally without it. Count how many of the tools on a real system resolve a repository but no matching tag, and whether the common failure is an unusual tag convention (`release-1.2`, a monorepo prefix) that a wider match would recover, or a genuine absence. Only then decide whether refusing stays.
- Report a failed tier-2 probe before falling through to synthesis. `a72610c` and `c14d425` removed repository worktrees from manpage discovery and limited synthesis checkouts to documentation paths, fixing the eCryptfs filename-limit failure observed on ruff. Other network, tag, tree, release or validation failures can still degrade to tier 3 without the outcome distinguishing "no repository resolved" from "repository resolved but unusable". A definitive absence remains a normal synthesis fallback. A transient network, cache, or validation failure must not silently generate a page that upstream may already ship; decide whether interactive installs ask before synthesis while non-interactive installs refuse, or whether every mode refuses with explicit `--generate` as the override.
- Tell a transparent wrapper from a genuinely different build, by comparing `--version` across `$PATH` occurrences. A wrapper of the `~/bin/overrides` shape (sources `_super`, strips its own directory from `$PATH`, `exec`s the underlying binary) matches no provider marker, so it is unclaimed and loses tiers 1 and 2 — an authoritative upstream manpage silently becomes an LLM call. Tier 3 still works, since `exec` passes `--help` and `--version` through to the real tool.
  ADR-0020 rejected the cheap fix of falling through to the next `$PATH` occurrence: "unclaimed" does not distinguish a wrapper from a different build shadowing a managed one, so falling through would attribute one binary's documentation to another with no evidence, which is the `fmt` -> `nushell/nufmt` failure in a new place.
  The evidence-based version is to compare `<first hit> --version` with `<next occurrence> --version`: a transparent wrapper reports identically because it execs through, a different build does not. That is evidence rather than a positional guess, so it satisfies ADR-0008/ADR-0015. Costs two subprocess runs per shadowed binary, ties when two builds share a version, and can be defeated by a wrapper that alters version output. Unbuilt because the scaffolding on the development system (`~/bin/overrides` holds only `_super` and `_template`) is not currently in use, so there is nothing live to test against.
  The machinery now exists: `crawler.get_version` (ADR-0021) runs a binary's `--version` and returns `None` on every failure mode, which is exactly the comparison primitive this needs. What is still missing is enumerating the *later* `$PATH` occurrences at all -- `loginpath.which_login` stops at the first hit by design, so a second entry point would be needed to see past it.
- **Resolved by `94e9410`, kept only for the measurement caveat.** `find_installation` and `enumerate_installations` used to disagree about which binary a name meant: `_resolve_bin_path` tried `~/.local/bin/<name>` first while `enumerate_installations` honoured `$PATH` precedence, so `maniac install <tool>` could act on a different binary than the shell runs. `$PATH` precedence is now the single rule and the implicit `~/.local/bin` default is gone -- it encoded one machine's layout, and the user has confirmed keeping tool symlinks there is a personal arrangement rather than a mise default. `discover_candidate_source`, a third variant, was dead and was deleted with it. ADR-0020 then moved the question again, from *which* `$PATH` to *whose*.
  **The measurement caveat this entry carried is now resolved.** It read: any figure measured through `uv run` in this repo -- including the 70-row / 45-upstream / 5-ok-14-available-51-missing counts -- was taken with `.venv/bin` shadowing real binaries, so a dev-dependency copy (`ty`, `ruff`, `pytest`) could stand in for the installed one. ADR-0020/ADR-0021 removed exactly that distortion: `$PATH` now comes from a login shell with the caller's environment scrubbed, so `uv run` no longer changes what MANIAC sees. Figures taken before that work still need re-measuring rather than trusting; figures taken after do not.
- Record the losing claims when two providers detect the same binary. `discovery.enumerate_installations` resolves each name at its first `$PATH` occurrence, which implements the tie-break correctly -- that is the binary that actually runs -- but it does this with a `seen` dict that skips later occurrences entirely, so a second provider's claim is discarded rather than kept. The retired roadmap decided "losers recorded, not discarded"; only the first half landed. The value is diagnostic: a user whose `$PATH` order hides a better-documented installation currently has no way to see that from MANIAC's output. Nothing depends on this today, which is why it was not built.
- The classification facts layer (`classification.py`) was deleted as unreferenced; recover it with `git log --diff-filter=D -- maniac/classification.py`.
- Serialize manifest writes; `maniac/manifest.py`'s read-modify-write loses an entry under concurrent installs. `load()` reads the whole manifest and `record()` writes it back, so two `maniac install` processes racing both read the same state and the second write drops the first's entry. The atomic `.tmp`-then-`Path.replace` in `_save` prevents a torn file but not a lost update -- both writes are individually atomic and one is simply overwritten. This is reachable through the workflow ADR-0013 deliberately composed, `maniac list --available --missing | xargs maniac install`, where `xargs -P` is a normal thing to type. The fix is an `flock(2)` on a sidecar lock file around the read-modify-write, as apt takes on `/var/lib/dpkg/lock`; `fcntl` is stdlib and MANIAC is POSIX-only, so no platform abstraction is needed. One design choice is open: apt fails fast ("could not get lock"), but under `xargs -P` a blocking acquire with a short timeout is friendlier, since the second process wants to wait its turn rather than error out. The window is small and the consequence is one page installed but untracked, recoverable by reinstalling, which is why this was recorded rather than fixed alongside ADR-0017 -- but note that change is what introduced the shared mutable state, so the race did not exist before it.

## Future milestones

Larger arcs deliberately deferred. Each is a decision already taken, not an oversight.

### Judge whether an existing manpage is worth replacing

Deferred by [ADR-0016](adr/0016-authoritative-manpages-first.md), which cut quality judgement from the current scope and removed `--candidates` and `min_words_per_flag` with it.
The gap this leaves is real: a poor vendor page MANIAC could improve is now invisible.
Reviving it means answering what ADR-0012 deferred and ADR-0014 could not carry -- a verdict layer over stored classification facts.
Two known obstacles are recorded below (`.HP` counting, and pages the ratio cannot rate at all).
This is also where "is the upstream page we just installed actually good?" belongs: ADR-0016 installs a shipped page without asking, on the grounds that upstream wrote it, and revisiting that is the same question in a different place.
The classification facts layer this milestone would call was deleted as unreferenced; recover it with `git log --diff-filter=D -- maniac/classification.py`.

### Judge whether a shipped upstream manpage is good enough

Follows from the milestone above but is separable.
`classification.py` already computes facts for any roff file regardless of where it lives, so the machinery to rate an install-root page before installing it exists; only the verdict is missing.
Worth doing once there is real output to judge -- some shipped pages are themselves help2man dumps, which `is_help2man_manpage` already detects and the tier-1 finder already rejects.

### Serve system and distro-packaged binaries

Deferred by decision, not blocked.
The provenance exists on Debian-family systems and is installation-derived, so it satisfies ADR-0008's rule: `dpkg-query -S <path>` gives the owning package, `dpkg-query -s <pkg>` gives `Homepage`, and `/usr/share/doc/<pkg>/copyright` gives the upstream `Source`.
Verified on the development system: `od` resolves to `coreutils`, `Homepage: gnu.org/software/coreutils`.
The documentation is local too, so this needs no network at all -- see the bounded-local-sources item above, and note that GNU info pages are the richest local source measured (`od`: 1053 words in the info node against 623 in the manpage, while the online 9.11 manual adds only two sentences over the installed 9.4).
Costs: it is `dpkg`-only (Fedora needs `rpm -qf`, Arch `pacman -Qo`, macOS nothing), and it explodes the candidate set from ~44 tools to thousands, so the lookups must be batched.
